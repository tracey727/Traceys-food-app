GENEVIEVE App™ FOOD V19.5 — ONE CLEAN DEPLOYMENT
=================================================

1. EXTRACT THE ZIP
Open the extracted folder. Do not upload the ZIP itself as the website.

2. REPLACE THE FOOD REPOSITORY ROOT
Upload every file and folder inside the extracted folder to the root of the FOOD APP repository.
Replace older files when asked.

3. DELETE THESE OLD ITEMS IF GITHUB STILL SHOWS THEM
- api folder
- sw.js
- package.json
- any old V19.2, V19.3 or V19.4 wrapper folder

4. CHECK THE ROOT
The first GitHub page must show index.html, app.js, styles.css, vercel.json, manifest.webmanifest and assets.

5. DEPLOY
Commit the replacement. Vercel should publish it as a static site with no install and no build command because the included vercel.json overrides those settings.

6. CONFIRM
Open the new deployment. The header must say V19.5.
Open Scan and use the Kikkoman photograph. It should detect 4901515129889 and load Kikkoman Gluten Free Soy Sauce 250 mL.

7. OLD IPHONE COPY
If an old Home Screen icon still says V19.3 or V19.4, remove only that old icon. Open the new V19.5 production URL in Safari and add it to the Home Screen again.
